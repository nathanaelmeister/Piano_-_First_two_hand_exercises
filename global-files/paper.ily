\paper {
  page-breaking = #ly:page-turn-breaking
  system-system-spacing =
  #'((basic-distance . 12)
     (minimum-distance . 10)
     (padding . 4)
     (stretchability . 60))

  tocItemMarkup = \tocItemWithDotsMarkup
}
